import React from 'react'
import CartClient from './components/CartClient'

type Props = {}

const page = (props: Props) => {
  return (
    <div>
      <CartClient />
    </div>
  )
}

export default page
